extends ../layout/layout

block prepend head
    - var page = ""
    - var title = "Gelukt check je email om te bevestigen"
    - var description = "De inschrijving is gelukt"
    - var path = "../"

block content
  section.alignsection.align
    h2(class="h2 error__h2") Bedank voor het invullen!
    img(src="../images/mail.svg" class="error__image" alt="")
    p(class="p error__p") 
      | Check je mail om je inschrijving voor de nieuwsbrief te bevestigen en de gratis PDF met tips en trucs te downloaden. 
      //- span.nextLine.nextLine--freePDF 
      //-   a(href="../gratis-tips.pdf" class="link") Gratis PDF met toegankelijkheids tips (449kb)
block script
    //- script(src=`${path}js/contactForm-bundle.js`)
    script(src=`${path}js/main-bundle.js`)


